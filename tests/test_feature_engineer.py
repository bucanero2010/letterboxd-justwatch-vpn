"""Tests for the Feature Engineer module.

Covers property-based tests for sparse TF-IDF construction invariants,
feature label reconstruction, embedding dimensionality, embedding cache round-trip,
item feature matrix concatenation shape, and metadata serialization round-trip.
"""
